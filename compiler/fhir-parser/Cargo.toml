[package]
name = "fhir-parser"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
fhir-ir = { path = "../fhir-ir" }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "2"

[dev-dependencies]
proptest = "1"
